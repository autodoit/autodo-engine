# support advice: autodo-engine -> kit-dependency-governance

## Context

`autodo-engine` supports `kit-dependency-governance` because `kit-dependency-governance` depends on `autodo-engine`.

## Adaptation Advice

Describe what `kit-dependency-governance` should update if `autodo-engine` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
