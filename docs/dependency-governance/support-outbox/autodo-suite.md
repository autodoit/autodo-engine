# support advice: autodo-engine -> autodo-suite

## Context

`autodo-engine` supports `autodo-suite` because `autodo-suite` depends on `autodo-engine`.

## Adaptation Advice

Describe what `autodo-suite` should update if `autodo-engine` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
