# 开发日志

## 2026-03-17

### autodo-engine / 拆分适配

- 将实际可发布包目录切换到 autodoengine，与 pyproject 和 CLI 入口对齐。
- 调整事务注册链路：官方事务目录改为从已安装的 autodo-kit 包定位。
- list_tools/get_tool 不再假设引擎仓内置 tools，改为桥接 autodokit.tools。
- README、用户手册、开发者指南、API 手册、产品文档改写为引擎仓口径。
- 删除误复制到引擎仓性 AOK预置事务手册副本。

## 2026-03-20

### autodo-engine / 能力增强

- 新增 `autodoengine.utils.skill_renderer` 模块，支持将 `SKILL.md`（YAML + Jinja2）参数化渲染为最终 Prompt。
- 环境适配：安装 `PyYAML` 和 `Jinja2` 核心依赖。
- 强化渲染安全性：默认使用 `StrictUndefined` 拦截未定义变量，支持多种模板搜索路径。
- 同步更新 `docs/API手册.md` 包含新渲染 API。

## 2026-03-25

### autodo-engine / tools public capability 平台落地

- 新增 `autodoengine/tools/atomic`、`autodoengine/tools/public`、`autodoengine/tools/adapters` 三层结构。
- 新增 `public_tools_manifest.json`、schema 集、统一返回协议、权限分级、审计落盘与 facade 执行链路。
- 首批落地 10 个 capability，并提供 CLI、Python、Agent、Runner 四类入口。
- 新增 `scripts/invoke_capability.py` 作为 IDE/Runner 统一脚本入口。
- 新增内置复合事务 `引擎能力巡检`、`引擎运行时探针`，通过 capability facade 完成工具编排对齐。
- 调整事务同步逻辑：增强模式下除 autodo-kit 事务外，也并入 engine 内置事务。
- 回归结果：`tests/test_public_capability_platform.py`、`tests/test_engine_standalone_mode.py`、`tests/test_affair_management.py` 共 14 项通过。
